<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Pop-Up</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Poppins', sans-serif;
            background-color: #121212;
            color: #FFF5E1;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .info-box {
            background-color: #1e1e1e;
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 0 20px rgba(228, 68, 63, 0.3);
            text-align: left;
            max-width: 500px;
            width: 90%;
        }

        h2 {
            color: #E4443F;
            margin-bottom: 0.5rem;
        }

        p {
            margin-top: 0;
            margin-bottom: 1.2rem;
            font-size: 1.1rem;
        }
    </style>
</head>
<body>
    <div class="info-box" id="output">
    </div>

    <script>
        function disp() {
            let sno = prompt("Enter your Student Number");
            let fname = prompt("Enter your First Name");
            let cs = prompt("Enter your Course");
            let sd = prompt("Enter your Subject Description");

            const container = document.getElementById("output");
            container.innerHTML = `
                <h2>Your Student Number:</h2>
                <p>${sno ? sno.toUpperCase() : "N/A"}</p>

                <h2>Your Full Name:</h2>
                <p>${fname ? fname.toUpperCase() : "N/A"}</p>

                <h2>Your Course and Section:</h2>
                <p>${cs ? cs.toUpperCase() : "N/A"}</p>

                <h2>Subject Description:</h2>
                <p>${sd ? sd.toUpperCase() : "N/A"}</p>
            `;
        }

        window.onload = disp;
    </script>
</body>
</html>
